{
  "install": "npm install && { [ -n \"$VITE_SUPABASE_URL\" ] || [ -f apps/web/.env ] || cp apps/web/.env.example apps/web/.env; }",
  "terminals": [
    {
      "name": "web",
      "command": "npm run dev --workspace=@carveout/web -- --host 0.0.0.0 --port 5173"
    }
  ]
}
